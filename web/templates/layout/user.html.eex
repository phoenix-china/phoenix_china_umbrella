<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Phoenix中文社区</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link "PhoenixChina", to: page_path(@conn, :index), class: "navbar-brand" %>
        </div>

        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><%= link "首页", to: page_path(@conn, :index) %></li>
          </ul>

          <ul class="nav navbar-nav navbar-right">
            <%= if @logged_in do %>
            <li><%= link @current_user.nickname, to: user_path(@conn, :show, @current_user.nickname) %></li>
            <li><%= link "退出登陆", to: session_path(@conn, :delete) %></li>
            <%= else %>
            <li><%= link "登陆", to: session_path(@conn, :new) %></li>
            <li><%= link "注册", to: user_path(@conn, :new) %></li>
            <%= end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>

    <div class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <div class="row">
        <div class="col-md-4">

          <div class="panel panel-default">
            <div class="panel-body">
              <div class="">
                <div class="media">
                  <div class="media-left">
                    <a href="#">
                      <img class="media-object img-circle img-thumbnail"
                        src="http://o7f5caqlk.bkt.clouddn.com/avatar.jpg"
                        alt="<%= @user.nickname %>"
                        style="width: 100px; height: 100px;">
                    </a>
                  </div>
                  <div class="media-body">
                    <h4 class="media-heading"><%= @user.nickname %></h4>
                    <div class="">第 <%= @user.id %> 位会员 / <%= @user.inserted_at |> Ecto.DateTime.to_erl |> Timex.from_now("ja") %>注册</div>
                    <div class="">
                      0 篇帖子 • 27 条回帖
                    </div>
                    <div class="">
                      <%= gettext "can't be blank" %>
                    </div>
                  </div>
                </div>
              </div>
              <div class="">
                <ul>
                  <li><a href="#">
                    <strong>0</strong>
                    <span>关注者</span>
                  </a></li>
                  <li><a href="#">
                    <strong>0</strong>
                    <span>正在关注</span>
                  </a></li>
                  <li><a href="#">
                    <strong>0</strong>
                    <span>收藏</span>
                  </a></li>
                </ul>
              </div>
            </div>
          </div>

        </div>

        <div class="col-md-8">
          <main role="main">
            <%= render @view_module, @view_template, assigns %>
          </main>
        </div>

      </div>


    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
