<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello PhoenixChina!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container" >
      <header class="header">
        <nav role="navigation" class="clearfix">
          <ul class="nav nav-pills pull-right">
            <%= if @logged_in do %>
            <li><%= link @current_user.nickname, to: "javascript:void(0);" %></li>
            <li><%= link "退出登陆", to: session_path(@conn, :delete) %></li>
            <%= else %>
            <li><%= link "登陆", to: session_path(@conn, :new) %></li>
            <li><%= link "注册", to: user_path(@conn, :new) %></li>
            <%= end %>
          </ul>
          <%= link "PhoenixChina", to: page_path(@conn, :index), class: "logo" %>
        </nav>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
